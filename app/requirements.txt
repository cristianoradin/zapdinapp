fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-socketio>=5.11.0
asyncpg>=0.29.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
playwright>=1.49.0
httpx>=0.28.0
python-jose[cryptography]>=3.3.0
itsdangerous>=2.2.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
Pillow>=10.0.0
qrcode>=7.4.0
# ── Ativação por token (criptografia AES-256-GCM) ────────────────────────────
cryptography>=42.0.0
# ── Kiosk window sem barra de endereços (WebView2) ──────────────────────────
# pywebview NÃO está aqui — instalado somente no ambiente de compilação do CI
# O serviço Windows (uvicorn) não usa pywebview; apenas o ZapDin-Launcher.exe compilado usa.
# Adicionar pywebview aqui causaria falha no pip install (pythonnet precisa de .NET SDK)
# ── Worker standalone (ZapDin-Worker.exe) ────────────────────────────────────
# httpx já incluído acima
